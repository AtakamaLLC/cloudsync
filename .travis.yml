language: python
python:
  - "2.6"
  - "2.7"
  - "3.5"
  - "3.6"
# command to install dependencies
install:
  - pip install pytest
# command to run tests
script:
  - pylint cloudsync
  - pytest tests

