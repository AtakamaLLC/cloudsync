language: python
python:
  - "3.6"
  - "3.7"
# command to install dependencies
install:
  - pip install pytest
  - pip install pylint
# command to run tests
script:
  - pylint cloudsync
  - pytest tests

